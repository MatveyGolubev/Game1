#include "animation.h"
/*
void legTimer(int value) {
    glutPostRedisplay();

    // поворота ноги

    if (legAngle > 45.0f) {
        legAngle = -15;
    }

    if (legAngle >= 45.0f || legAngle <= -15) {
        legStep = -legStep; // Изменение шага поворота
    }

    glutTimerFunc(10, legTimer, 0); // Изменение таймера
}
*/
